<UserControl x:Class="LiveDescribe.Controls.DescriptionRecordingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:converters="clr-namespace:LiveDescribe.Converters">
    
    <UserControl.Resources>
        <Style
            x:Key="ControlPanelStyle"
            TargetType="Control">
            <Setter
                Property="Margin"
                Value="5" />
        </Style>
        <converters:BoolToRecordingContentConverter x:Key="BoolToRecordingContentConverter"/>
    </UserControl.Resources>
    
    <Grid>
        <StackPanel>
            <CheckBox
                IsChecked="{Binding ExtendedIsChecked}" />
            
            <Button
                Command="{Binding RecordCommand}"
                Style="{StaticResource ControlPanelStyle}"
                Content="{Binding Path=IsRecording,
                Converter={StaticResource BoolToRecordingContentConverter}}"
                Width="100" />
        </StackPanel>
    </Grid>
</UserControl>
