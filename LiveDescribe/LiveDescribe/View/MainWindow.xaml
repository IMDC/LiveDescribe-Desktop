<Window x:Class="LiveDescribe.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        Title="MainWindow" Height="800" Width="1280" MinHeight="800" MinWidth="1280">


    <DockPanel>
        <!--Top Menu Bar-->
        <Menu DockPanel.Dock="Top" HorizontalAlignment="Stretch">
            <MenuItem Header="File">
                <MenuItem  DataContext="{Binding VideoControl}" Header="Import Video" Command="{Binding ImportVideoCommand}">
                </MenuItem>
            </MenuItem>
        </Menu>
        
        <!--Main Grid -->
        <Grid DockPanel.Dock="Bottom"  Background="White">

            <!--Main Grid Row Definitions-->    
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>

            <!--Main Grid Column Definitions-->
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition />
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <!--Media Element-->
            <Border Background="Black" Grid.Row="0" Grid.RowSpan="7" Grid.ColumnSpan="4">
                <MediaElement DataContext="{Binding VideoControl}"  Name="VideoMedia" Source="{Binding Path}" LoadedBehavior="Manual">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="MediaOpened">
                            <i:InvokeCommandAction Command="{Binding VideoOpened}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </MediaElement>
            </Border>

            <!--Button Controls for Video PLayer-->
            <StackPanel  DataContext="{Binding VideoControl}" Orientation="Horizontal" Grid.Row="7" Grid.ColumnSpan="4" Grid.Column="0">
                <Button Margin="11" Height="30" Command="{Binding MuteCommand}">Mute</Button>
                <Slider Value="{Binding Path=Volume, ElementName=VideoMedia}" Name="VolumeControl" Maximum="1" Minimum="0"  Margin="15"
                        Width="100"/>
                <Button Command="{Binding RecordCommand}" Margin="11" Height="30">Record</Button>
                <Button Command="{Binding RewindCommand}" Margin="11" Height="30">Rewind</Button>
                <Button Name="PlayControl" Margin="11" Height="30" Command="{Binding PlayCommand}">Play</Button>
                <Button Name="PauseControl" Margin="11" Height="30" Command="{Binding PauseCommand}">Pause</Button>
                <Button Command="{Binding FastForwardCommand}" Margin="11" Height="30">Fast Forward</Button>
            </StackPanel>
            
            <!--Description Information-->
            <TabControl Grid.Row="0" Grid.Column="4" Grid.RowSpan="6" Grid.ColumnSpan="3">
                <TabItem Header="Spaces">
                </TabItem>
                <TabItem Header="Descriptions">        
                </TabItem>
                <TabItem Header="Extended Descriptions">
                </TabItem>
            </TabControl>
            
            <!--Timeline-->
            <Border Grid.Column="0" BorderBrush="Black" Grid.ColumnSpan="7"  Grid.Row="8" Grid.RowSpan="5" BorderThickness="2"  VerticalAlignment="Stretch">
                <ScrollViewer SizeChanged="timeLine_SizeChanged_1" Name="TimeLine"  HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>


                       <!-- <StackPanel Grid.ColumnSpan="6"  Grid.Row="8" Grid.RowSpan="5">-->

                        <Border Name="AudioCanvasBorder"  Grid.Row="0" Grid.RowSpan="2"  BorderBrush="Black" BorderThickness="1">
                            <Canvas Name="AudioCanvas" VerticalAlignment="Top" >
                                <Polyline Points="10,20, 0,0,  20,0, 10,20, 10,187"   RenderOptions.EdgeMode="Aliased" Stroke="Black" Fill="Black" StrokeThickness="2" Width="20"  Canvas.Left="-10" Canvas.Top="0" Name="Marker">
                                    
                                </Polyline>
                            </Canvas>
                        </Border>

                        <Border Name="DescriptionCanvasBorder" Grid.Row="2" BorderBrush="Black" BorderThickness="1" >
                            <Canvas Name="DescriptionCanvas">
                            
                            </Canvas>
                        </Border>
                        
                        
                    <!--</StackPanel>-->
                    </Grid>
                </ScrollViewer>
            </Border>

            <!--Description Text Box-->
            <TextBox Grid.Column="4" Grid.Row="6" Grid.ColumnSpan="2" Grid.RowSpan="2" ScrollViewer.VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" Text="Enter Description Text" />

            <!--Text Controls-->
            <StackPanel Grid.Column="6" Grid.Row="6" Grid.RowSpan="2">
               
                <Button Margin="10">
                    Save Text
                </Button>
                <Button Margin="10">
                    Clear
                </Button>
            </StackPanel>
            
        </Grid>

    </DockPanel>
</Window>
